{
  "name": "ui5.walkthrough",
  "version": "1.0.0",
  "description": "The UI5 walkthrough application",
  "scripts": {
         "start": "concurrently \"ui5 serve --port 8080\" \"browser-sync start --proxy 'localhost:8080' --files 'webapp/**/*' --inject-changes\""
  },
  "devDependencies": {
    "@ui5/cli": "^4.0.4",
    "browser-sync": "^3.0.2",
    "concurrently": "^8.2.2"
  }
}
